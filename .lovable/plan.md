## Что исправлю

1. Сделаю так, чтобы кнопки не выглядели «мертвыми» без объяснения.
2. Переделаю блок маршрута: не просто ряд точек, а более читаемую визуальную схему с направлением, сегментами и понятным текущим этапом.
3. Приведу админку в рабочее состояние для мобильного экрана и верну нормальные действия для доп. заданий.

## Конкретно по проблеме с неактивной кнопкой

Сейчас на главной кнопка «Передать в штаб» выглядит активной по стилю, но фактически бесполезна, пока не выбраны файл и город. В коде это не объясняется пользователю.

Исправлю так:
- добавлю явный текст-подсказку под формой, что нужно выбрать файл, указать город и позывной;
- переведу кнопку в честное состояние: disabled только когда реально не хватает обязательных полей;
- добавлю визуально понятный disabled-state, чтобы было ясно, почему она недоступна;
- аналогично проверю и поправлю кнопки в комментариях, кабинете агента и в админке.

## Что сделаю с картой

Сейчас это действительно больше похоже на линейную инфографику, а не на «карту/чертёж». Переделаю компонент маршрута в сторону схемы:

- добавлю фон как у тактического планшета;
- разобью маршрут на выраженные участки с направлением движения;
- выделю текущую точку и пройденные этапы более ясно;
- уберу визуальный акцент с непонятных чисел вида `01/06`, если они не помогают;
- заменю/сокращу вторичные подписи, которые сейчас выглядят как случайные даты или служебные метки;
- сделаю мобильную компоновку, где схема читается в 464px ширины, без ощущения «просто шесть кружков в ряд».

Если потребуется, схема станет не строго горизонтальной, а более маршрутной: с изломами, секциями и подписями этапов.

## Что исправлю в админке

По текущему состоянию видно, что в админке доступна только авторизация по мастер-паролю, а после входа часть действий завязана на серверные функции и текущее содержимое таблиц. Также для дополнительных заданий сейчас в базе есть только публичные чтение/создание, а обновления/удаления напрямую таблицей запрещены — поэтому проверю весь поток через серверные функции и поправлю интерфейс так, чтобы действия были реально доступны.

Сделаю:
- проверку всех admin action-кнопок на мобильной ширине;
- исправление состояния кнопок, которые визуально есть, но недоступны или неудобны из-за layout;
- нормальный блок для доп. заданий: добавление, список, удаление, а при необходимости — привязка к городу;
- после каждого действия — мгновенное обновление списка и понятный feedback.

## Технически

Затрону:
- `src/components/RouteMap.tsx`
- `src/components/PhotoFeed.tsx`
- `src/routes/admin.tsx`
- при необходимости `src/server/admin.functions.ts`
- при необходимости типы/API-слой для дополнительных заданий
- возможно миграцию, если для доп. заданий нужна привязка к городу или новый формат данных

## Результат

После правок:
- кнопки будут либо реально рабочими, либо с понятной причиной, почему они пока недоступны;
- маршрут станет похож на читаемую схему, а не на набор точек;
- админка снова позволит управлять дополнительными заданиями и связанными настройками с телефона.

Подтверждай — и я сразу внесу правки.